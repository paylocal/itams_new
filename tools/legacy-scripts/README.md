# Legacy Utility Scripts

This folder contains one-off and maintenance scripts moved from the project root to keep the workspace clean.

Notes:
- These scripts are not used by Next.js runtime/build directly.
- Review each script before running in production.
- Prefer adding repeatable automation under `scripts/` with documentation if needed.
